<!DOCTYPE html>
<html lang="en">

<head>
    <%-include('includes/head-config') %>
    <link rel="stylesheet" href="/styles/review-booking.css">
    <title>DevelopAir</title>
</head>

<body>
    <%- include('includes/nav-bar')%>
    <%- include('includes/side-drawer')%>    
    <div id="pax-greeting">
        <h2>Hello, <strong id="booked-pax-name"><%= reservationData.paxName %></strong></h2>
        <h3>Here's the itinerary you have booked with us under the confirmation code <span
                id="booked-confirmation-code"><strong><%= reservationData.reservationCode %></strong></span></h3>
    </div>
    <h3 class="section-title">Passenger data</h3>
    <div id="booked-passenger-data">
        <p>Passenger name: <strong><%= reservationData.paxName %></strong></p>
        <p>Passenger last name: <strong><%= reservationData.paxLastName %></strong></p>
        <p>Passenger date of birth: <strong><%= reservationData.paxDateOfBirth %></strong></p>
        <p>Passenger email address: <strong><%= reservationData.paxEmail %></strong></p>
        <p>Passenger phone number: <strong><%= reservationData.paxPhone %></strong></p>
        <p>Passenger country of residence: <strong><%= reservationData.paxCountry %></strong></p>
        <p>Number of seats booked: <strong><%= reservationData.reservedSeats %></strong></p>
        <p>Amount paid: <strong>$0</strong></p>
    </div>
    <h3 class="section-title">Your itinerary</h3>
    <section id="booked-itinerary">
        <section class="booked-flight-information">
            <span class="type-of-flight">Outbound flight:</span>
            <%-include('includes/reserved-flight-details',{
                departureDate:reservationData.outboundDepartureDate,
                flightNumber:reservationData.outboundFlightNumber,
                origin: reservationData.outboundOrigin,
                originCity: reservationData.outboundOriginCity,
                originName: reservationData.outboundOriginName,
                departureTime:reservationData.outboundDepartureTime,
                destination: reservationData.outboundDestination,
                destinationCity: reservationData.outboundDestinationCity,
                destinationName: reservationData.outboundDestinationName,
                arrivalTime:reservationData.outboundArrivalTime,
            }) %>
        </section>

        <% if(reservationData.inboundOrigin != null) { %>
        <section class="booked-flight-information">
            <span class="type-of-flight">Inbound flight:</span>
            <%-include('includes/reserved-flight-details',{
                departureDate:reservationData.inboundDepartureDate,
                flightNumber:reservationData.inboundFlightNumber,
                origin: reservationData.inboundOrigin,
                originCity: reservationData.inboundOriginCity,
                originName: reservationData.inboundOriginName,
                departureTime:reservationData.inboundDepartureTime,
                destination: reservationData.inboundDestination,
                destinationCity: reservationData.inboundDestinationCity,
                destinationName: reservationData.inboundDestinationName,
                arrivalTime:reservationData.inboundArrivalTime,
            }) %>
        </section>
        <% } %>
    </section>
    <p id="farewell">If you wish to make changes to your itinerary, please call us at our contact center.</p>
</body>

</html>
